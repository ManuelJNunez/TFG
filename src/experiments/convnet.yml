name: 'Simple CNN'
kind: 'group'

params:
  epochs: 10
  seed: 2342

param_space:
  bs: randint(32, 256)
  lr: loguniform(0.01, 0.00001)
  out_channels:
    - randint(2, 20)
    - randint(2, 20)

run:
  - src/convnet.py
